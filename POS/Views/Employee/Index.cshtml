@model List<POS.Models.Employee>

<div class="page-wrapper">
    <div class="content">
        <div class="row">
            <div class="col-sm-12">
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h4 class="card-title">Employee List</h4>
                        <a asp-action="Create" class="btn btn-primary rounded"><i class="fa fa-plus"></i> Create</a>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table datatable table-bordered">
                                <thead class="thead-light" >
                                    <tr>
                                        <th width="20%" class="text-center">SR</th>
                                        <th>Name</th>
                                        <th width="20%" class="text-center">Action</th>
                                    </tr>
                                </thead>
                                <thead class="custom">
                                    <tr>
                                        <th class="p-1"><input type="text" class="form-control form-control-sm input-dt-search" placeholder="Search SR" /></th>
                                        <th class="p-1"><input type="text" class="form-control form-control-sm input-dt-search" placeholder="Search Name" /></th>
                                        <th class="p-1 text-center"></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @{ int SR = 1; }
                                    @foreach (var item in Model.Where(x => x.IsDelete == 0).OrderByDescending(x => x.Id))
                                    {
                                        <tr>
                                            <td class="text-center">@SR</td>
                                            <td>@item.Name</td>
                                            <td class="text-center">
                                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary btn-sm" title="Edit"><i class="fas fa-pencil"></i></a>
                                              @if(item.Id>1)
                                              {  <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm" title="Delete"><i class="fas fa-trash"></i></a>
                                         }   </td>
                                        </tr>
                                        SR++; 
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>